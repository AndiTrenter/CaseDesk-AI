# CaseDesk AI Frontend Configuration
# For local development only

# Backend URL (leave empty for production - nginx handles routing)
REACT_APP_BACKEND_URL=http://localhost:8001

# WebSocket port (only for development server)
WDS_SOCKET_PORT=3000

# Enable health check endpoints (development only)
ENABLE_HEALTH_CHECK=false
